#Create a variable "string" that contains the text "This is just an example". Depending on the position of each letter within the string,
#calculate what value (x,y) we should add to select only the word 'example'.
#string [x,y] = "example"

string = "This is just an example"
print (string[16:])